---
name: mu-phrase-ui-copy
description: 为产品界面编写、评审和落地清晰可信的中英文文案。适用于按钮、空状态、错误、权限、订阅、数据状态、本地化资源和无障碍文本；不用于营销文章或与文案无关的代码审查。
---

# MU LABS Phrase · UI 文案

MU LABS / Mustundead

让人看懂眼前发生的事，知道下一步能做什么，以及这个选择会改变什么。

这是 Mustundead 的界面写作方法。默认语气自然、平实、具体；重视事实和人的感受，保留产品自己的性格。简短是一种结果，不是删掉必要信息的理由。

## 先匹配工作范围

| 用户要什么 | 如何完成 |
| --- | --- |
| 改一句话 | 结合已知场景给出一个推荐版本。行为不明时说明适用条件。无需扫描整个项目。 |
| 评审页面或流程 | 阅读可用界面和相关行为，指出有证据的问题、建议及影响；评审本身不授权改文件。 |
| 实现文案修改 | 找到文案来源、共享调用和实际状态，修改约定的界面与语言，验证运行结果。 |
| 全产品审计 | 先建立明确的页面与状态清单，再逐项检查；报告范围外和未验证部分。 |
| 修改本 skill | 交付技能文件；不顺带修改正在打开的产品。 |

以当前请求、产品术语表、用户指定措辞和项目约定为准。普通措辞偏好由上下文决定；只有缺失信息会改变事实或行动后果时才澄清。已有授权无需分阶段重复确认。

## 写之前确认四件事

1. **此刻的任务。** 人要找什么、判断什么、完成什么？把最影响决定的信息放在能看到的位置。
2. **当前事实。** 文字由哪种状态触发？信息来自哪里？动作实际调用什么？只知道截图时，不把推断当行为证据。
3. **行动边界。** 对象是谁，何时生效，影响本机、云端还是第三方？能否撤销？关键后果必须在选择之前出现。
4. **上下文。** 标题、正文、按钮和辅助技术读出的内容是否共同表达完整意思？通知和小组件需要能独立理解。

用一句话概括本次约束即可，例如：“只改连接卡片的刷新失败状态；保留上次读数和时间；不修改授权流程。”不要为一个按钮制造一份长报告。

## MU LABS 的写法

- 用人平时会说的动词和具体对象。目的地回答“去哪儿”，按钮回答“按下去做什么”。流程足够明确时，保留“继续”“完成”等常规用语。
- 保留能帮助判断的限制、范围和时间。不要把“请求已发送”写成“已完成”，或把“最近一次读取”写成实时值。
- 一句承载一个主要意思。先说明事实，再给有用的行动。错误不指责人，不用空泛安慰代替恢复方法。
- 同一概念统一名称，不同概念保留差别。不要因为字数相近就让“连接”“授权”“同步”互换。
- 语气随情境变化：日常操作简洁，失败状态平静，真实的完成时刻可以有温度。没有通用禁词表。
- 中文和英文分别写得自然。保留产品名、系统术语、变量、标点含义；不把英文大小写和空格规则机械套到中文。
- 不为塞进固定宽度而删除重要后果。先判断是否应换行、增加空间或调整组件，并在当前范围内处理。

## 数据与后果必须说实话

对于用量、同步、备份、订阅和多来源界面，先确认下列区别，再写文案：

| 概念 | 不可省略的区别 |
| --- | --- |
| 数值 | 未获取不是零；估算不是实测；不同单位和统计周期不能直接合计。 |
| 新鲜度 | 旧值仍有用，但应显示其时间；刷新失败不会自动证明数据丢失。 |
| 来源 | 某一来源失败不代表其他来源失败；来源身份和当前账户要能区分。 |
| 权限 | 能连接不代表有访问权限；退出登录、断开连接、撤销授权不是同一动作。 |
| 权益 | 提供方额度、提供方订阅和产品自己的付费权益分别表达。 |
| 删除 | 清除本地缓存、删除云端记录、移除来源配置分别核对处理函数。 |
| 购买 | 已发起、等待批准、已验证、已恢复分别表达；以实际交易状态和商品数据为依据。 |

不得为文案好看而编造原因、完成时间、重试入口、设置跳转、自动恢复或隐私保证。条件不足时给出带条件的候选版本。

## 按需要打开参考

- 按钮、弹窗、错误、权限、购买：[组件与状态](references/components-and-states.md)。
- `.xcstrings`、`.strings`、ICU、复数、变量、中英文及可访问名称：[本地化与无障碍](references/localization-and-accessibility.md)。
- TokenBell、ByteKibble、ByTouch、KinView 的教学情境：[MU LABS 示例](references/examples.md)。示例的前提必须先成立，不代表产品已上线的行为。
- 系统术语或平台约定不确定：[资料与取舍](references/sources.md)。按具体问题查当前官方文档，不必每次做研究。

## 实现时走到实际界面

**定位来源。** 阅读直接拥有文案的组件、语言资源和动作处理。区分应用自己写的文本、系统控件、第三方内容、用户内容与受约束的条款。检查每个受影响的共享键；必要时拆分语义不同的键。

**保护语言契约。** 只重写约定语言，但共享代码新增键时检查所有受影响语言的解析路径。保持参数身份与类型、复数/设备分支、转义和译者备注。源代码会重新提取资源时，修正真正的源头。不要顺手迁移本地化系统。

**验证修改。** 解析实际改过的资源，运行项目已有的相关检查；应用源文件或打包资源有变化时构建受影响目标。检查完整 diff，确认没有顺带改键、语言或行为。

**看见结果。** 在当前运行版本中检查改变含义或布局的状态。选择相关的长内容、窄宽度、字体大小和语言；不用穷举不受影响的组合。无障碍名称或读出顺序改变时，用辅助功能检查或真实读屏验证。构建成功和截图都不能证明读屏体验。

## 交付尺度

一句话修改直接给最终文字。评审或实现记录 `界面/状态 · 文件或键 · 修改与理由 · 证据 · 状态`，保留正确文案，不凑问题数量。

清楚区分建议、已修改、已运行验证和已发布。设备、登录或运行环境缺失时，完成可做的检查，明确哪一步尚无证据。达到约定结果后停止；不要再靠更多版本、更短字数或主观评分延长任务。
